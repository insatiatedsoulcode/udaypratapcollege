### Programs (`app/academics/programs/page.tsx`)

- Lists BBA, BCA, BA with features and links to detail pages.
- Includes `KeyFeatureSection` and a CTA that opens the Inquiry modal.

#### Example
```tsx
import ProgramsPage from '@/app/academics/programs/page';
// Use as a normal Next.js route at /academics/programs
```
